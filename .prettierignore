# Don't format the HTML template as it's used in tests and formatting may break them
.github/actions/collect-rss-feeds/template.html

# Don't format generated HTML feed pages (they contain duplicate template code)
docs/feed-*.html

# Don't format the main index and summary (they are generated)
docs/index.html
docs/summary.html

# Don't format JavaScript as we use JavaScript Standard Style (conflicts with Prettier)
docs/script.js
