{
  "name": "rss-feed-collection",
  "version": "1.0.0",
  "description": "RSS Feed Collection with mark as read functionality",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/docs/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "docs/script.js"
    ]
  }
}
